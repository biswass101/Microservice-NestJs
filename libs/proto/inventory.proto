syntax = "proto3";

package inventory;

service InventoryService {
    rpc CheckStock(StockRequest) returns 
    (StockResponse);
}


message StockRequest {
    string productId = 1;
}

message StockResponse {
    int32 availableQuantity = 1;
    bool inStock = 2;
}









